# Core Unsloth (fastest & compatible with Llama 3.1 8B 4-bit)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Compatible versions that work perfectly together (Nov 2025)
trl==0.24.0
peft==0.12.0
transformers==4.57.2
accelerate==0.34.0
bitsandbytes==0.44.0
datasets==2.21.0

# Evaluation & processing
sacrebleu==2.4.2
rouge_score==0.1.2
pandas==2.2.2
torch==2.6.0

# Optional but useful
sentencepiece
protobuf
